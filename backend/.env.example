PORT=3000
JWT_SECRET=change_me_dev_secret

# Public URL of the frontend (used in invoice links)
PUBLIC_URL=http://localhost:8015
FRONTEND_PORT=8015

# SMTP settings for sending invoice emails on order completion
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=example_user
SMTP_PASS=example_password
EMAIL_FROM=no-reply@nsm-koreacars.com